"""
Write a Python program that calculates the area of a rectangle using variables for
length and width, and then displays the result.
"""
length = float(input("Length : "))
width = float(input("Width : "))
area = length*width
print(f"Area of rectangle is {area}.")